@using Domain_Layer.DTOs
@using Domain_Layer.DTOs.ProductDtos
@using Domain_Layer.DTOs._ِCategoryDtos
@model PaginatedListDto<GetAllProductsDto>

@{
    ViewData["Title"] = "Luxury Shop - Shop Store";
    var searchTerm = Context.Request.Query["search"];
    var sortBy = Context.Request.Query["sortBy"];
    var sortDir = Context.Request.Query["sortDir"];
    var categoryId = Context.Request.Query["categoryId"];
}

@section Styles {
    <style>
        /* (نفس الـ Styles الموجودة في السؤال الأول) */
        :root {
            --primary-pink: #d68a8a;
            --dark-bg: #0f0f0f;
            --card-bg: #1a1a1a;
            --input-bg: #252525;
            --text-light: #ffffff;
            --text-muted: #a0a0a0;
            --accent-gold: #c5a059;
            --danger-red: #ff4d4d;
            --success-green: #2ecc71;
        }

        body {
            background-color: var(--dark-bg);
            color: var(--text-light);
            font-family: 'Inter', sans-serif;
            padding-top: 20px;
        }

        .filter-section {
            background: var(--card-bg);
            border-radius: 12px;
            padding: 15px;
            margin-bottom: 25px;
            border: 1px solid #252525;
        }

        .form-control-custom {
            background: var(--input-bg) !important;
            border: 1px solid #333 !important;
            color: var(--text-light) !important;
            border-radius: 8px;
            padding: 8px 12px;
            font-size: 0.9rem;
            transition: all 0.3s ease;
        }

            .form-control-custom:focus {
                border-color: var(--primary-pink) !important;
                box-shadow: 0 0 0 3px rgba(214, 138, 138, 0.1) !important;
            }

        .input-group-text {
            background: var(--input-bg) !important;
            border: 1px solid #333 !important;
            color: var(--text-muted) !important;
            border-radius: 8px 0 0 8px !important;
            font-size: 0.9rem;
        }

        .product-card {
            background: var(--card-bg);
            border-radius: 16px;
            border: 1px solid #252525;
            overflow: hidden;
            transition: all 0.3s ease;
            height: 100%;
            display: flex;
            flex-direction: column;
            max-width: 350px;
            margin: 0 auto;
        }

            .product-card:hover {
                transform: translateY(-5px);
                border-color: var(--primary-pink);
                box-shadow: 0 15px 30px rgba(214, 138, 138, 0.15);
            }

        .img-wrapper {
            aspect-ratio: 1/1;
            background: #0a0a0a; /* لون داكن موحد بدون تدرج */
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow: hidden;
        }

        .product-img {
            width: 100%;
            height: 100%;
            object-fit: cover; /* تغطية كاملة بدون فراغات */
            transition: transform 0.5s ease;
        }

        .product-card:hover .product-img {
            transform: scale(1.05);
        }

        .product-info {
            padding: 18px 15px 15px;
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        .product-category {
            color: var(--primary-pink);
            font-size: 0.75rem;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 6px;
        }

        .product-name {
            font-family: 'Playfair Display', serif;
            font-size: 1.2rem;
            font-weight: 600;
            color: var(--text-light);
            margin-bottom: 10px;
            line-height: 1.3;
            height: 55px;
            overflow: hidden;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
        }

        .product-description {
            color: var(--text-muted);
            font-size: 0.9rem;
            line-height: 1.5;
            margin-bottom: 18px;
            flex: 1;
            height: 65px;
            overflow: hidden;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
        }

        .product-actions {
            margin-top: 15px;
            display: flex;
            flex-direction: column;
            gap: 0;
        }

        .admin-actions {
            display: flex;
            gap: 8px;
            margin-bottom: 12px;
            padding-bottom: 12px;
            border-bottom: 1px solid #333;
        }

        .btn-admin {
            flex: 1;
            padding: 6px 10px;
            border-radius: 6px;
            text-align: center;
            text-decoration: none;
            font-weight: 600;
            font-size: 0.8rem;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 5px;
            cursor: pointer;
            border: none;
        }

        .btn-edit {
            background: rgba(214, 138, 138, 0.1);
            border: 1px solid var(--primary-pink);
            color: var(--primary-pink);
            text-decoration: none;
        }

            .btn-edit:hover {
                background: var(--primary-pink);
                color: white;
                text-decoration: none;
            }

        .btn-delete {
            background: rgba(255, 77, 77, 0.1);
            border: 1px solid var(--danger-red);
            color: var(--danger-red);
        }

            .btn-delete:hover {
                background: var(--danger-red);
                color: white;
            }

        .btn-details {
            background: transparent;
            color: var(--text-light);
            border: 2px solid var(--primary-pink);
            border-radius: 10px;
            padding: 12px;
            font-weight: 600;
            text-align: center;
            text-decoration: none;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px;
            width: 100%;
            font-size: 0.95rem;
        }

            .btn-details:hover {
                background: var(--primary-pink);
                color: white;
                transform: translateY(-2px);
                box-shadow: 0 8px 15px rgba(214, 138, 138, 0.2);
            }

        .empty-state {
            text-align: center;
            padding: 50px 20px;
        }

        .empty-state-icon {
            font-size: 3.5rem;
            color: var(--primary-pink);
            margin-bottom: 15px;
            opacity: 0.5;
        }

        .pagination {
            margin-top: 40px;
            justify-content: center;
        }

        .page-link {
            background: var(--card-bg);
            border: 1px solid #333;
            color: var(--text-light);
            padding: 8px 15px;
            margin: 0 4px;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

            .page-link:hover {
                border-color: var(--primary-pink);
                color: var(--primary-pink);
            }

        .page-item.active .page-link {
            background: var(--primary-pink);
            border-color: var(--primary-pink);
            color: white;
        }

        .page-item.disabled .page-link {
            background: #222;
            border-color: #333;
            color: #666;
        }

        .swal2-popup {
            background: #1a1a1a !important;
            border: 1px solid #252525 !important;
            border-radius: 16px !important;
            color: #ffffff !important;
        }

        .swal2-title {
            color: #ffffff !important;
            font-family: 'Playfair Display', serif !important;
        }

        .swal2-html-container {
            color: #a0a0a0 !important;
        }

        .swal2-confirm {
            background: linear-gradient(45deg, #d68a8a, #c27676) !important;
            border: none !important;
            border-radius: 8px !important;
        }

        .swal2-cancel {
            background: transparent !important;
            border: 1px solid #444 !important;
            color: #ffffff !important;
            border-radius: 8px !important;
        }

        .product-card.removing {
            animation: slideOut 0.5s ease forwards;
        }

        @@keyframes slideOut {
            0% {
                opacity: 1;
                transform: translateY(0) scale(1);
            }

            100% {
                opacity: 0;
                transform: translateY(-20px) scale(0.9);
                height: 0;
                margin: 0;
                padding: 0;
                border: none;
            }
        }

        .custom-toast {
            position: fixed;
            top: 20px;
            right: 20px;
            background: var(--card-bg);
            color: white;
            padding: 15px 20px;
            border-radius: 10px;
            border-left: 4px solid var(--danger-red);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
            z-index: 99999;
            display: flex;
            align-items: center;
            gap: 12px;
            transform: translateX(150%);
            transition: transform 0.4s ease;
            max-width: 350px;
        }

        .toast-show {
            transform: translateX(0);
        }

        .toast-icon {
            color: var(--danger-red);
            font-size: 1.3rem;
        }

        .toast-content h4 {
            margin: 0 0 3px 0;
            font-size: 1rem;
        }

        .toast-content p {
            margin: 0;
            color: var(--text-muted);
            font-size: 0.85rem;
        }

        @@media (max-width: 768px) {
            .filter-section {
                padding: 12px;
            }

            .product-name {
                font-size: 1.1rem;
                height: 50px;
            }

            .product-description {
                height: 60px;
                font-size: 0.85rem;
            }

            .btn-details {
                padding: 10px;
                font-size: 0.9rem;
            }

            .img-wrapper {
                aspect-ratio: 1/1;
            }
        }

        /* تحسين عرض الصور على الشاشات الصغيرة */
        @@media (max-width: 576px) {
            .product-card {
                max-width: 100%;
            }
        }
    </style>
}

<!-- Hidden Anti-Forgery Token -->
<input type="hidden" id="RequestVerificationToken" name="__RequestVerificationToken"
       value="@Html.AntiForgeryToken()" />

<!-- Toast Notification for Error Messages -->
@if (TempData["ErrorMessage"] != null)
{
    <div id="errorToast" class="custom-toast">
        <div class="toast-icon">
            <i class="bi bi-exclamation-triangle"></i>
        </div>
        <div class="toast-content">
            <h4>Error</h4>
            <p>@TempData["ErrorMessage"]</p>
        </div>
    </div>
}

<div class="container">
    <!-- Filter Section -->
    <div class="filter-section">
        <form id="filterForm" method="get" asp-action="GetAllProducts" class="row g-2 align-items-center">
            <!-- حقل البحث -->
            <div class="col-12 col-md-5 col-lg-4">
                <div class="input-group">
                    <span class="input-group-text">
                        <i class="bi bi-search"></i>
                    </span>
                    <input type="text"
                           name="search"
                           value="@searchTerm"
                           class="form-control form-control-custom"
                           placeholder="Search products..."
                           aria-label="Search products">
                </div>
            </div>

            <!-- Dropdown التصنيفات (Categories) -->
            <div class="col-6 col-md-3 col-lg-3">
                <select name="categoryId" class="form-select form-control-custom" onchange="this.form.submit()">
                    <option value="">All Categories</option>
                    @if (ViewBag.Categories != null)
                    {
                        foreach (var cat in ViewBag.Categories)
                        {
                            <option value="@cat.Id" selected="@(categoryId == cat.Id.ToString())">
                                @cat.Name
                            </option>
                        }
                    }
                </select>
            </div>

            <!-- القائمة المدمجة للترتيب -->
            <div class="col-6 col-md-4 col-lg-3">
                <select id="sortSelect" class="form-select form-control-custom">
                    <option value="newest" selected="@(sortBy == "id" && sortDir == "desc")">Newest First</option>
                    <option value="nameAsc" selected="@(sortBy == "name" && sortDir == "asc")">Name (A to Z)</option>
                </select>
                <!-- حقول مخفية تحمل قيم sortBy و sortDir الفعلية -->
                <input type="hidden" name="sortBy" id="sortByHidden" value="@sortBy" />
                <input type="hidden" name="sortDir" id="sortDirHidden" value="@sortDir" />
            </div>
        </form>
    </div>

    <!-- Products Grid -->
    <div class="row g-4" id="productsGrid">
        @if (Model.Items != null && Model.Items.Any())
        {
            @foreach (var product in Model.Items)
            {
                <div class="col-12 col-sm-6 col-md-4 col-lg-3" id="product-@product.Id">
                    <div class="product-card">
                        <!-- Product Image -->
                        <div class="img-wrapper">
                            @if (!string.IsNullOrEmpty(product.ImageUrl))
                            {
                                <img src="@product.ImageUrl"
                                     class="product-img"
                                     alt="@product.Name"
                                     loading="lazy"
                                     onerror="this.src='https://via.placeholder.com/400x400/1a1a1a/d68a8a?text=Pro+Makeup+Studio'">
                            }
                            else
                            {
                                <div class="d-flex flex-column align-items-center justify-content-center text-center text-muted h-100">
                                    <i class="bi bi-image fs-1 mb-2"></i>
                                    <small>No Image</small>
                                </div>
                            }
                        </div>

                        <!-- Product Info -->
                        <div class="product-info">
                            <!-- Admin Actions -->
                            @if (User.IsInRole("Admin"))
                            {
                                <div class="admin-actions">
                                    <a asp-action="Edit"
                                       asp-route-id="@product.Id"
                                       class="btn-admin btn-edit"
                                       title="Edit Product">
                                        <i class="bi bi-pencil"></i>
                                        <span>Edit</span>
                                    </a>

                                    <button type="button"
                                            class="btn-admin btn-delete delete-product-btn"
                                            data-product-id="@product.Id"
                                            data-product-name="@product.Name">
                                        <i class="bi bi-trash"></i>
                                        <span>Delete</span>
                                    </button>
                                </div>
                            }

                            <!-- Product Name -->
                            <h3 class="product-name" title="@product.Name">
                                @product.Name
                            </h3>

                            <!-- Product Description -->
                            @if (!string.IsNullOrEmpty(product.Description))
                            {
                                <div class="product-description" title="@product.Description">
                                    @{
                                        var shortDescription = product.Description.Length > 150
                                        ? product.Description.Substring(0, 150) + "..."
                                        : product.Description;
                                    }
                                    @shortDescription
                                </div>
                            }
                            else
                            {
                                <div class="product-description text-muted">
                                    <i>No description available</i>
                                </div>
                            }

                            <!-- Product Actions -->
                            <div class="product-actions">
                                <a asp-action="Details"
                                   asp-route-id="@product.Id"
                                   class="btn-details">
                                    <i class="bi bi-eye-fill"></i>
                                    <span>View Product Details</span>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <!-- Empty State -->
            <div class="col-12">
                <div class="empty-state">
                    <div class="empty-state-icon">
                        <i class="bi bi-search"></i>
                    </div>
                    <h3 class="mb-3">No Products Found</h3>
                    <p class="text-muted mb-4">
                        @if (!string.IsNullOrEmpty(searchTerm))
                        {
                            <span>No results found for "<strong>@searchTerm</strong>". Try a different search term.</span>
                        }
                        else
                        {
                            <span>There are currently no products available in our luxury collection.</span>
                        }
                    </p>
                    @if (!string.IsNullOrEmpty(searchTerm) || !string.IsNullOrEmpty(categoryId))
                    {
                        <a asp-action="GetAllProducts" class="btn btn-outline-primary">
                            <i class="bi bi-arrow-left me-2"></i>
                            Clear Filters
                        </a>
                    }
                </div>
            </div>
        }
    </div>

    <!-- Pagination -->
    @if (Model.TotalPages > 1)
    {
        <nav class="pagination-section">
            <ul class="pagination">
                <!-- Previous Page -->
                <li class="page-item @(Model.PageNumber == 1 ? "disabled" : "")">
                    <a class="page-link"
                       asp-action="GetAllProducts"
                       asp-route-pageIndex="@(Model.PageNumber - 1)"
                       asp-route-search="@searchTerm"
                       asp-route-sortBy="@sortBy"
                       asp-route-sortDir="@sortDir"
                       asp-route-categoryId="@categoryId"
                       aria-label="Previous">
                        <i class="bi bi-chevron-left"></i>
                    </a>
                </li>

                <!-- Page Numbers -->
                @for (int i = 1; i <= Model.TotalPages; i++)
                {
                    if (i == 1 || i == Model.TotalPages || (i >= Model.PageNumber - 2 && i <= Model.PageNumber + 2))
                    {
                        <li class="page-item @(i == Model.PageNumber ? "active" : "")">
                            <a class="page-link"
                               asp-action="GetAllProducts"
                               asp-route-pageIndex="@i"
                               asp-route-search="@searchTerm"
                               asp-route-sortBy="@sortBy"
                               asp-route-sortDir="@sortDir"
                               asp-route-categoryId="@categoryId">
                                @i
                            </a>
                        </li>
                    }
                    else if (i == Model.PageNumber - 3 || i == Model.PageNumber + 3)
                    {
                        <li class="page-item disabled">
                            <span class="page-link">...</span>
                        </li>
                    }
                }

                <!-- Next Page -->
                <li class="page-item @(Model.PageNumber == Model.TotalPages ? "disabled" : "")">
                    <a class="page-link"
                       asp-action="GetAllProducts"
                       asp-route-pageIndex="@(Model.PageNumber + 1)"
                       asp-route-search="@searchTerm"
                       asp-route-sortBy="@sortBy"
                       asp-route-sortDir="@sortDir"
                       asp-route-categoryId="@categoryId"
                       aria-label="Next">
                        <i class="bi bi-chevron-right"></i>
                    </a>
                </li>
            </ul>

            <!-- Page Info -->
            <div class="text-center text-muted mt-3">
                Showing page @Model.PageNumber of @Model.TotalPages
                @if (Model.Items != null && Model.Items.Any())
                {
                    <span class="mx-2">•</span>
                    <span>@Model.Items.Count() items</span>
                }
            </div>
        </nav>
    }
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <script>
        // دالة عرض Toast
        function showToast(message, type = 'success') {
            const existingToast = document.querySelector('.custom-toast:not(#errorToast)');
            if (existingToast) existingToast.remove();

            const toast = document.createElement('div');
            toast.className = 'custom-toast';
            toast.style.borderLeftColor = type === 'success' ? '#2ecc71' : '#ff4d4d';

            toast.innerHTML = `
                <div class="toast-icon">
                    <i class="bi ${type === 'success' ? 'bi-check-circle' : 'bi-exclamation-triangle'}"></i>
                </div>
                <div class="toast-content">
                    <h4>${type === 'success' ? 'Success' : 'Error'}</h4>
                    <p>${message}</p>
                </div>
            `;

            document.body.appendChild(toast);
            setTimeout(() => toast.classList.add('toast-show'), 100);
            setTimeout(() => {
                toast.classList.remove('toast-show');
                setTimeout(() => toast.remove(), 400);
            }, 3000);
        }

        // دالة لحذف المنتج باستخدام AJAX
        async function deleteProduct(productId, productName) {
            const result = await Swal.fire({
                title: 'Delete Luxury Product?',
                html: `Are you sure you want to delete <strong>"${productName}"</strong>?<br>This action cannot be undone.`,
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#d68a8a',
                cancelButtonColor: '#6c757d',
                confirmButtonText: 'Yes, delete it!',
                cancelButtonText: 'Cancel'
            });

            if (!result.isConfirmed) return;

            try {
                const token = document.querySelector('input[name="__RequestVerificationToken"]').value;
                const formData = new FormData();
                formData.append('id', productId);

                const response = await fetch('@Url.Action("Delete", "Products")', {
                    method: 'POST',
                    headers: { 'RequestVerificationToken': token },
                    body: formData
                });

                const data = await response.json();

                if (data.success) {
                    const productElement = document.getElementById(`product-${productId}`);
                    if (productElement) {
                        const card = productElement.querySelector('.product-card');
                        card.classList.add('removing');
                        setTimeout(() => {
                            productElement.remove();
                            if (document.querySelectorAll('.product-card').length === 0) {
                                window.location.reload();
                            }
                        }, 500);
                    }
                    showToast(data.message || 'Luxury product deleted successfully!', 'success');
                } else {
                    showToast(data.message || 'Failed to delete product.', 'error');
                }
            } catch (error) {
                console.error('Error:', error);
                showToast('An error occurred while deleting the product.', 'error');
            }
        }

        // معالجة تغيير القائمة المدمجة للترتيب
        document.getElementById('sortSelect').addEventListener('change', function() {
            const value = this.value;
            if (value === 'newest') {
                document.getElementById('sortByHidden').value = 'id';
                document.getElementById('sortDirHidden').value = 'desc';
            } else if (value === 'nameAsc') {
                document.getElementById('sortByHidden').value = 'name';
                document.getElementById('sortDirHidden').value = 'asc';
            }
            document.getElementById('filterForm').submit();
        });

        document.addEventListener('DOMContentLoaded', function() {
            // Show error toast if exists
            const errorToast = document.getElementById('errorToast');
            if (errorToast) {
                errorToast.classList.add('toast-show');
                setTimeout(() => {
                    errorToast.classList.remove('toast-show');
                    setTimeout(() => errorToast.remove(), 400);
                }, 5000);
            }

            // Lazy loading animation for product cards
            const productCards = document.querySelectorAll('.product-card');
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0)';
                    }
                });
            }, { threshold: 0.1 });

            productCards.forEach(card => {
                card.style.opacity = '0';
                card.style.transform = 'translateY(20px)';
                card.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
                observer.observe(card);
            });

            // Add delete event listeners
            document.querySelectorAll('.delete-product-btn').forEach(button => {
                button.addEventListener('click', function() {
                    deleteProduct(this.dataset.productId, this.dataset.productName);
                });
            });

            // Lazy loading images
            const imageObserver = new IntersectionObserver((entries, observer) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        const img = entry.target;
                        img.src = img.dataset.src || img.src;
                        observer.unobserve(img);
                    }
                });
            });

            document.querySelectorAll('.product-img').forEach(img => {
                if (!img.complete) imageObserver.observe(img);
            });
        });
    </script>
}